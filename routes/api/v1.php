<?php

declare(strict_types=1);

use Illuminate\Support\Facades\Route;

/*
|--------------------------------------------------------------------------
| API v1 — Dawar (REST)
|--------------------------------------------------------------------------
|
| Register versioned endpoints here. Prefer thin controllers that delegate
| to module Actions / Services.
|
*/

// Route::prefix('storefront')->group(...);
// Route::prefix('merchant')->middleware(...)->group(...);
